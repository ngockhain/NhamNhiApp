import categorySlice from "./reducers";

import * as categoryOperations from "./operations";
import * as categorySelectors from "./selectors";

export {
    categoryOperations,
    categorySelectors,
};

export default categorySlice;
